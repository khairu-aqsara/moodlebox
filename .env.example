# Code Signing Configuration (Optional)
# Copy this file to .env and fill in your values to enable code signing
# If not set, builds will be unsigned (works fine for development and CI/CD)

# ===== macOS Code Signing =====
# Developer ID Application certificate name
# Find with: security find-identity -v -p codesigning | grep "Developer ID"
# Example: CSC_NAME="Developer ID Application: Your Name (TEAM_ID)"
CSC_NAME=

# Apple Developer account email (for notarization)
APPLE_ID=

# Apple app-specific password (generate at https://appleid.apple.com)
# Required for notarization
APPLE_APP_SPECIFIC_PASSWORD=

# Apple Developer Team ID (found in Apple Developer account)
APPLE_TEAM_ID=

# ===== Windows Code Signing =====
# Path to .pfx certificate file, or certificate name from Windows Certificate Store
# Example: CSC_LINK="/path/to/certificate.pfx"
# Or: CSC_NAME="Your Certificate Name" (for certificate store)
CSC_LINK=

# Password for .pfx certificate (if password-protected)
CSC_KEY_PASSWORD=

# ===== Linux GPG Signing (Optional) =====
# Linux packages can be GPG signed, but it's optional
# Most Linux distributions don't require code signing
